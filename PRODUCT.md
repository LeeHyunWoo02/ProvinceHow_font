# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

주 사용자는 **수도권에 거주하며 지방 이주를 고려하는 청년**이다. 배경 상황: 수도권 주거비 상승, 원격 근무 확산으로 지역 분산 근무가 현실적 선택지가 됨. 이들이 하려는 일(job)은 자신의 조건(희망 산업·직무, 예산, 통근 시간, 생활 선호도)에 맞는 지방 정착 후보지를 탐색하고, 여러 지역을 비교한 뒤, 실제 상담·신청까지 이어가는 것이다.

## Product Purpose

정부·지자체·공공데이터(고용24, 국토교통부, LOCALDATA 등)에 흩어져 있는 지방 이주 관련 정보를 한 화면에서 통합 제공하여, "생각만 하던 지방 이주"를 실제 행동으로 전환시키는 것이 목적이다. 성공은 사용자가 탐색에서 멈추지 않고 지자체 공식 페이지로 이동해 상담·신청 단계까지 도달하는 것으로 정의된다.

## Positioning

경쟁 서비스가 쉽게 따라 하기 어려운 지점은 **정보 통합 + 조건 기반 자동 추천 + 동일 시점·동일 단위 비교의 결합**이다:
- 여러 공공데이터 출처를 하나의 지역 단위로 통합
- 산업·직무·예산·통근·생활 선호도 기반 자동 필터링과 AI 추천(픽 이유 포함)
- 선택한 지역들을 동일 지표로 나란히 비교
- 지자체 공식 페이지로의 원클릭 이동으로 탐색→행동 전환

## Operating Context

- 사용자는 지도 또는 조건 검색으로 시군구를 선택하고, 해당 지역의 일자리·주거비·지원사업·생활 인프라를 한 화면에서 확인한다.
- 여러 지역을 선택해 비교 뷰에서 핵심 지표를 나란히 본다.
- 최종 행동은 지자체 공식 페이지로 이동해 상담·신청하는 것이며, 이 저장소 밖에서 일어난다.
- 데이터는 전부 별도 저장소의 Spring Boot 백엔드에서 온다. 이 프론트엔드 저장소에는 서버 코드가 없다.
- 지도는 외부 지도 SDK 없이 `src/assets/*.json`의 SVG path 데이터를 직접 렌더링한다.
- 배포는 Vercel이며 SPA rewrite로 모든 경로를 `/`로 보낸다.

## Capabilities and Constraints

- 화면: 지도 검색(MapSearch), 조건 검색(DetailSearch), 지역 상세(RegionInfo), 지역 비교(Comparison).
- 지역 데이터 모델에 일자리(JobInfo·JobVacancy·산업별 비중), 주거비(월세/전세 중위·평균), 생활 인프라(건강·식생활·문화·생활, 항목별 점수), 지원사업(제목·URL·키워드), AI 추천 픽과 추천 사유가 포함된다.
- 백엔드 API는 **부분 연동 상태**다 — 일부 화면은 실제 공공데이터(고용24·국토교통부·LOCALDATA 등)와 연동되어 있고, 일부는 아직 목데이터로 채워져 있다. 특정 필드나 화면이 실데이터인지 목데이터인지는 이 저장소만으로 확정할 수 없으므로, 실데이터 여부를 전제로 한 주장(수치·개수 등)을 새로 지어내지 않는다.
- 접근성: 별도로 지정된 요구사항 없음. 일반적인 웹 접근성 수준(시맨틱 마크업, 키보드 접근성, 색 대비 등)을 따른다.
- 지원 브라우저/디바이스에 대한 별도 제약 없음.

## Brand Commitments

- 제품명: **ProvinceHow(지방어때)**.
- 로고 자산: `src/assets/logo.png`.

## Evidence on Hand

- `README.md`에 팀 소개, 핵심 목표, 주요 기능, 차별점, 시스템 아키텍처(프론트엔드/백엔드/DB/인프라/CI-CD)가 문서화되어 있다.
- 실제 사용자 인터뷰, 테스트베드 지표, 언론 보도 등 정량적 근거는 없다. 향후 작업에서 이런 증거를 지어내지 않는다.
- 백엔드는 Spring Boot(Java 17, Gradle) + Docker + AWS EC2 + Redis + Amazon RDS로 별도 저장소에서 운영되며, AWS API Gateway·Nginx·GitHub Actions CI/CD를 사용한다(README 근거, 이 저장소의 소스는 아님).

## Product Principles

1. **탐색에서 행동까지 이어지는 짧은 경로를 최우선한다** — 정보를 보여주는 것 자체가 목표가 아니라 지자체 신청·상담으로의 전환이 목표다.
2. **여러 출처의 정보를 동일 지역·동일 시점 단위로 통합해 보여준다** — 흩어진 공공데이터를 사용자가 직접 짜맞추게 하지 않는다.
3. **사용자 조건(예산·직무·통근·생활 선호)을 중심으로 화면을 구성한다** — 일반적인 지역 정보 나열이 아니라 개인화된 추천이 기본값이다.
4. **비교는 항상 동일 지표·동일 단위로 이루어진다** — 지역마다 다른 형식으로 정보를 보여주지 않는다.
5. **실데이터와 목데이터가 섞여 있는 현재 단계를 인지하고, 화면이 암시하는 확정성(수치, 개수, 최신성)을 과장하지 않는다.**

## Accessibility & Inclusion

특별히 지정된 요구사항 없음. 일반적인 웹 접근성 수준을 따른다.
